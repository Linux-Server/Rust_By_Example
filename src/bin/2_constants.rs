fn main(){
  // constants are immutable by default
  //it should be snakecase with capital lettor
  // The type must be annoted for constants- mandatory
  const SAM_RAY : i32 = 100;
  println!("The constant value is {:?}", SAM_RAY);
}